/**
 * Asset URLs must respect Vite's base path so the site works when hosted
 * under a subpath (e.g. GitHub Pages at /Scrapper/).
 */
export const MONO_FONT_URL = `${import.meta.env.BASE_URL}fonts/jetbrains-mono-400.woff`;
